export interface TaskFilters {
  status?: string;
  priority?: string;
  department?: string;
  assignee?: string;
}

export interface ProjectFilters {
  status?: string;
  department?: string;
}

// ... rest of the existing types ...